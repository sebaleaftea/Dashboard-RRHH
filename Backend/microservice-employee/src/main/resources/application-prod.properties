# Perfil de producción para Cloud Run con Cloud SQL

# Cloud SQL connection via Unix socket (Cloud Run)
spring.datasource.url=jdbc:postgresql://${DB_HOST:34.176.82.205}:${DB_PORT:5432}/${DB_NAME:grh}
spring.datasource.username=${DB_USERNAME:grh-bdduser}
spring.datasource.password=c_}2ysUR"6dXEk]o
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.show-sql=false

# Flyway - deshabilitado porque ya aplicamos DDL manualmente
spring.flyway.enabled=false

# Server
server.port=${PORT:8080}

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
